┌─────────────────┐
│  Your IoT App   │
│  (JSON payload) │
└────────┬────────┘
         │ compress-payload.sh (gzip -9)
         ▼
┌─────────────────┐
│ background-sync │──► Queue to disk (batching)
│    (queue)      │
└────────┬────────┘
         │ Every 15 min: flush
         ▼
┌─────────────────┐
│  firewall-rules  │──► Block telemetry, rate limit
│  (iptables)      │
└────────┬────────┘
         ▼
┌─────────────────┐
│  network-tweaks  │──► MTU=1280, TTL=65, TCP opt
│  (iproute2)      │
└────────┬────────┘
         ▼
┌─────────────────┐
│   EMnify APN     │──► Roaming enabled, IMEI auth
│  (rmnet_data0)   │
└─────────────────┘
         ▼
    [Mobile Network]